---
## [10:10] Gap-5 — 대형 Java 파일 파싱 오류 수정 (errors 430 → 0)

**완료한 작업:**
- 근본 원인 규명: CRLF가 아닌 tree-sitter 0.21.x Node.js 바인딩의 **32,767자 한계** (2^15 - 1)
- `parseFile()` 에서 대형 파일(≥32,768자)은 callback API(4096자 청크)로 우회
- BOM/CRLF 정규화 추가 (방어적 처리, `replace(/^\uFEFF/,'').replace(/\r\n?/g,'\n')`)
- `IndexResult`에 `errorPaths: string[]` 추가 — 실패 파일 목록 사후 조회 가능
- CLI `--verbose` 모드에서 실패 파일 최대 20개 경로 출력
- bts 재인덱싱 검증: **errors 430 → 0** (indexed 46,975, 316초)

**변경된 파일:**
- `src/indexer/tree-sitter/parser.ts` — 핵심 수정 (32K 한계 우회 callback API)
- `src/indexer/indexer.ts` — `errorPaths` 수집
- `src/cli/index.ts` — verbose 시 실패 파일 경로 출력
- `src/indexer/tree-sitter/__tests__/parser.test.ts` — CRLF/BOM/32K 단위 테스트
- `src/indexer/__tests__/indexer.test.ts` — 통합 테스트

**다음 단계:**
- Gap-3b: Full index 메모리 최적화 (피크 7.4 GB → 배치 INSERT 개선)
- JS/TS 심볼 추출: `extractFromJS` 신규 구현

**참조:**
- 커밋: `85b2764`
- bts 성능 보고서: `dev-logs/2026-04-14-gap3-perf-report.md`
---
---
## [20:57] JS/TS 심볼 추출기 구현 (extractFromJS) — 전체 완료

**완료한 작업:**
- `tree-sitter-javascript@0.21.4`, `tree-sitter-typescript@0.21.2` 설치 (tree-sitter 0.21.x 호환)
- `parser.ts`: `SupportedLanguage` 확장 (`javascript|typescript|tsx`), `detectLanguage` JS/TS 확장자 분기 추가, 4개 lazy 파서 싱글톤
- `js-extractor.ts` 신규 구현 (`extractFromJS`): 심볼(function/class/method/getter/setter/constructor/field/variable/interface/type_alias/enum/namespace/default_export), ESM import/extends/implements, CommonJS require+module.exports, JSX 컴포넌트 ref, TS 타입 레벨 ref
- `indexer.ts`: `DEFAULT_EXTENSIONS` 추가 (9개 JS/TS 확장자), if/else 디스패치 확장
- 단위 테스트 53개 (`js-extractor.test.ts`), 통합 테스트 8개 (`indexer.test.ts`) 추가
- `parser.test.ts`: JS/TS 라우팅 + 32K 스트리밍 테스트 추가
- **vitest 291/291 통과, tsc clean, build 성공**

**변경된 파일:**
- `package.json` — tree-sitter-javascript@0.21.4, tree-sitter-typescript@0.21.2 추가
- `src/indexer/tree-sitter/parser.ts` — SupportedLanguage 확장, 4개 파서 싱글톤, detectLanguage 확장
- `src/indexer/tree-sitter/js-extractor.ts` — 신규 (extractFromJS 구현)
- `src/indexer/indexer.ts` — DEFAULT_EXTENSIONS 상수, JS/TS 디스패치
- `src/indexer/tree-sitter/__tests__/js-extractor.test.ts` — 신규 (53개 테스트)
- `src/indexer/tree-sitter/__tests__/parser.test.ts` — JS/TS 라우팅 테스트 추가
- `src/indexer/__tests__/indexer.test.ts` — JS/TS 통합 테스트 추가

**다음 단계:**
- 커밋 & 푸시
- bts 프로젝트 재인덱싱으로 E2E 검증 (JS/TS 심볼 count 증가 확인)
- Kotlin 파서 연결 (kotlin-extractor.ts stub이 이미 존재)

**이슈/메모:**
- tree-sitter-javascript 0.21.x에서 `method_definition`의 get/set은 `childForFieldName('kind')`로 접근 불가 — children에서 직접 탐색 필요
- tree-sitter-javascript에서 class 표현식의 노드 타입은 `class` (not `class_expression`)
- TS `namespace` 선언은 tree-sitter-typescript에서 `expression_statement > internal_module`로 래핑됨
- JS `class_heritage`는 `extends` 키워드 + identifier 직접 구조 (TS와 달리 `extends_clause` 래퍼 없음)
---
## [19:59] Gap-3b — 인덱서 메모리/성능 최적화 (실측 결과 기록)

**완료한 작업:**
- `resolveProjectRefs` JS Map/배열 로딩 → 순수 SQL UPDATE 전환 (534K+ rows JS 로드 제거)
- `tree.delete()` finally 블록 추가 (tree-sitter native heap 명시 해제)
- prepared statement WeakMap 캐시 추가 (800K+ prepare → 5회)
- 배치 트랜잭션 도입 (batchSize=10, 46K commit → 4,697 commit)
- 베이스라인 재측정으로 핵심 사실 발견:
  - 기존 보고서 7.4 GB는 **430개 파일 실패 상태**의 수치 (Gap-5 이전)
  - Gap-5 이후 실제 베이스라인: **10.4 GB** (46,975 파일, 579K 심볼, 0 errors)
  - Gap-3b 후: **10.3 GB, 161s** (베이스라인 대비 ~100 MB 절감, 23s 단축)
- vitest 221/221 통과, tsc clean, build 성공

**변경된 파일:**
- `src/storage/queries.ts` — statement 캐시 helper, resolveProjectRefs SQL 전환, 핫패스 cached() 적용
- `src/indexer/indexer.ts` — batchSize 옵션, tree.delete() finally, 배치 트랜잭션

**다음 단계:**
- commit & push (Gap-3b)
- dev-logs/ 성능 보고서 작성
- JS/TS 심볼 추출 (extractFromJS) 또는 Kotlin 파서 연결

**이슈/메모:**
- 목표(7.4→3-4 GB)는 달성 못했으나, 이는 목표 설정 시 잘못된 베이스라인 사용 때문
- 실제 개선: 속도 12.5% 향상(184→161s), 메모리 소폭(100 MB) 절감
- 메모리 주 원인: Gap-5로 430개 대형 파일 성공 파싱 → 심볼 +8.6% (534K→579K)
- Gap-3b 구현 자체는 올바르고 코드 품질 향상에 기여
---
---
## [23:10] Vue SFC 지원 추가 (extractFromVue) — .vue 파일 심볼 인덱싱

**완료한 작업:**
- tree-sitter-vue 0.2.1 호환성 검증 → language 프로퍼티 누락으로 tree-sitter 0.21.x 비호환 확인
- regex two-pass 전략으로 대체: `<script>`/`<script setup>` 블록 추출 → 기존 extractFromJS 위임
- `detectLanguage()`에 `.vue` → `'vue'` 추가, `parseFile`에서 vue null 반환
- `vue-extractor.ts` 신규 구현: detectScriptLang, extractScriptBlocks, extractFromVue
- 라인 오프셋 보정 (raw_text 시작 행 기준), Vue 3 이중 블록 병합
- `indexer.ts`: DEFAULT_EXTENSIONS에 .vue 추가, 분기, null tree finally guard 수정
- TDD로 진행: parser 3개 + vue-extractor 15개 + indexer E2E 5개 테스트 작성
- vitest 312/312 통과, tsc clean, build 성공

**변경된 파일:**
- `src/indexer/tree-sitter/parser.ts` — SupportedLanguage 확장, .vue 감지, parseFile null guard
- `src/indexer/tree-sitter/vue-extractor.ts` — 신규 (extractFromVue 구현)
- `src/indexer/tree-sitter/__tests__/vue-extractor.test.ts` — 신규 (15개 테스트)
- `src/indexer/tree-sitter/__tests__/parser.test.ts` — .vue 감지 테스트 3개 추가
- `src/indexer/indexer.ts` — DEFAULT_EXTENSIONS, import, 분기, null guard 수정
- `src/indexer/__tests__/indexer.test.ts` — Vue SFC E2E 통합 테스트 5개 추가

**다음 단계:**
- bts 프로젝트 재인덱싱으로 .vue 심볼 count E2E 검증 (실제 Nuxt/Vue 프로젝트 필요)
- Kotlin 파서 연결 (kotlin-extractor.ts stub 이미 존재)

**이슈/메모:**
- tree-sitter-vue 0.2.1은 node_modules/tree-sitter-vue/build/Release/ 바이너리 빌드는 되지만
  language 프로퍼티를 export하지 않음 — tree-sitter 0.21.x ABI 변경으로 인한 비호환
- null.delete?.() TypeError: TypeScript cast(null as unknown as T)는 런타임에서 null 그대로 —
  .delete?.() optional chaining은 call에만 적용, property access에는 적용 안 됨 → if (tree) guard로 수정
- 커밋: e6b0116
---
