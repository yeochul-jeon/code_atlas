---
## [10:10] Gap-5 — 대형 Java 파일 파싱 오류 수정 (errors 430 → 0)

**완료한 작업:**
- 근본 원인 규명: CRLF가 아닌 tree-sitter 0.21.x Node.js 바인딩의 **32,767자 한계** (2^15 - 1)
- `parseFile()` 에서 대형 파일(≥32,768자)은 callback API(4096자 청크)로 우회
- BOM/CRLF 정규화 추가 (방어적 처리, `replace(/^\uFEFF/,'').replace(/\r\n?/g,'\n')`)
- `IndexResult`에 `errorPaths: string[]` 추가 — 실패 파일 목록 사후 조회 가능
- CLI `--verbose` 모드에서 실패 파일 최대 20개 경로 출력
- bts 재인덱싱 검증: **errors 430 → 0** (indexed 46,975, 316초)

**변경된 파일:**
- `src/indexer/tree-sitter/parser.ts` — 핵심 수정 (32K 한계 우회 callback API)
- `src/indexer/indexer.ts` — `errorPaths` 수집
- `src/cli/index.ts` — verbose 시 실패 파일 경로 출력
- `src/indexer/tree-sitter/__tests__/parser.test.ts` — CRLF/BOM/32K 단위 테스트
- `src/indexer/__tests__/indexer.test.ts` — 통합 테스트

**다음 단계:**
- Gap-3b: Full index 메모리 최적화 (피크 7.4 GB → 배치 INSERT 개선)
- JS/TS 심볼 추출: `extractFromJS` 신규 구현

**참조:**
- 커밋: `85b2764`
- bts 성능 보고서: `dev-logs/2026-04-14-gap3-perf-report.md`
---
---
## [19:59] Gap-3b — 인덱서 메모리/성능 최적화 (실측 결과 기록)

**완료한 작업:**
- `resolveProjectRefs` JS Map/배열 로딩 → 순수 SQL UPDATE 전환 (534K+ rows JS 로드 제거)
- `tree.delete()` finally 블록 추가 (tree-sitter native heap 명시 해제)
- prepared statement WeakMap 캐시 추가 (800K+ prepare → 5회)
- 배치 트랜잭션 도입 (batchSize=10, 46K commit → 4,697 commit)
- 베이스라인 재측정으로 핵심 사실 발견:
  - 기존 보고서 7.4 GB는 **430개 파일 실패 상태**의 수치 (Gap-5 이전)
  - Gap-5 이후 실제 베이스라인: **10.4 GB** (46,975 파일, 579K 심볼, 0 errors)
  - Gap-3b 후: **10.3 GB, 161s** (베이스라인 대비 ~100 MB 절감, 23s 단축)
- vitest 221/221 통과, tsc clean, build 성공

**변경된 파일:**
- `src/storage/queries.ts` — statement 캐시 helper, resolveProjectRefs SQL 전환, 핫패스 cached() 적용
- `src/indexer/indexer.ts` — batchSize 옵션, tree.delete() finally, 배치 트랜잭션

**다음 단계:**
- commit & push (Gap-3b)
- dev-logs/ 성능 보고서 작성
- JS/TS 심볼 추출 (extractFromJS) 또는 Kotlin 파서 연결

**이슈/메모:**
- 목표(7.4→3-4 GB)는 달성 못했으나, 이는 목표 설정 시 잘못된 베이스라인 사용 때문
- 실제 개선: 속도 12.5% 향상(184→161s), 메모리 소폭(100 MB) 절감
- 메모리 주 원인: Gap-5로 430개 대형 파일 성공 파싱 → 심볼 +8.6% (534K→579K)
- Gap-3b 구현 자체는 올바르고 코드 품질 향상에 기여
---
