---
## [11:20] E2E 검증 — JS/TS/Vue 심볼 추출 (cjonstyle-nuxt-frontweb)

**완료한 작업:**
- cjonstyle-nuxt-frontweb 프로젝트 인덱싱으로 JS/TS/Vue E2E 검증
- 버그 발견: `config/loader.ts` `DEFAULT_EXTENSIONS`가 `.java`만 포함 → CLI가 config 기본값을 indexer에 전달하므로 JS/TS/Vue 파일 미수집
- 수정: `config/loader.ts` `DEFAULT_EXTENSIONS`를 `indexer.ts`와 동기화 (`.java` + 9개 JS/TS/Vue 확장자)
- 재인덱싱 결과: **890 파일, 9,308 심볼, 에러 0, 5.9s**

**인덱싱 결과 (cjonstyle-nuxt-frontweb):**

| 확장자 | 파일 수 | 심볼 수 |
|--------|--------|--------|
| `.vue` | 468    | 6,071  |
| `.ts`  | 410    | 3,184  |
| `.js`  | 12     | 53     |

심볼 종류: variable 4,454 / function 2,268 / field 1,603 / method 482 / interface 250 / class 64 / 기타

**변경된 파일:**
- `src/config/loader.ts` — DEFAULT_EXTENSIONS `.java` only → 전체 확장자 동기화

**홀드 결정:**
- Kotlin 파서 연결: 현재 프로젝트 스펙 아웃 — 성숙도 향상 후 재검토

**커밋:**
- `fix: config DEFAULT_EXTENSIONS에 JS/TS/Vue 확장자 누락 수정`
- `docs: E2E 검증 작업 로그 추가`
---
---
## [11:50] 프로젝트 메모리/지식 저장소 추가 (Serena MCP 스타일)

**완료한 작업:**
- Serena MCP의 `.serena/memories/` 시스템에서 영감을 받아 CodeAtlas에 지식 저장소 구현
- 마크다운 파일 기반 (YAML frontmatter + 본문) — 사람이 읽고 편집 가능, git 추적 가능
- 저장 경로: `<project-root>/.codeatlas/memories/<slug>.md`

**구현 내용:**

| 모듈 | 내용 |
|------|------|
| `src/memory/store.ts` | 파일시스템 CRUD (write/read/list/edit/delete) |
| `src/memory/onboarding.ts` | AI 자동 온보딩 — 첫 인덱싱 시 4개 메모리 자동 생성 |
| `src/mcp/memory-tools.ts` | MCP 도구 어댑터 (프로젝트 해석 + store 래퍼) |
| `src/config/loader.ts` | `memory` 설정 섹션 추가 (enabled/path/autoOnboard) |
| `src/mcp/server.ts` | 5개 MCP 도구 등록 (tools 20-24) |
| `src/cli/index.ts` | `memories` 서브커맨드 + 온보딩 훅 추가 |

**MCP 도구 (5개 추가, 총 24개):**
- `write_memory` / `read_memory` / `list_memories` / `edit_memory` / `delete_memory`

**CLI 커맨드:**
- `codeatlas memories <project>` — 메모리 목록
- `codeatlas memories <project> read <slug>` — 메모리 읽기
- `codeatlas memories <project> delete <slug>` — 메모리 삭제
- `codeatlas memories <project> onboard` — 수동 온보딩 트리거

**자동 온보딩:**
- `codeatlas index` 완료 후 메모리가 없으면 자동 실행 (`ANTHROPIC_API_KEY` 필요)
- 생성 메모리: `project-overview` / `architecture` / `entry-points` / `conventions`
- claude-haiku-4-5 사용 (빠르고 저렴)

**테스트:**
- `store.test.ts` 31개 + `onboarding.test.ts` 11개 + `memory-tools.test.ts` 17개 = 59개 신규
- 전체: **374/374 통과** (기존 312 → +62), tsc clean

**변경된 파일:**
- `src/memory/store.ts` (신규)
- `src/memory/onboarding.ts` (신규)
- `src/mcp/memory-tools.ts` (신규)
- `src/memory/__tests__/store.test.ts` (신규)
- `src/memory/__tests__/onboarding.test.ts` (신규)
- `src/mcp/__tests__/memory-tools.test.ts` (신규)
- `src/config/loader.ts` — memory 섹션 추가
- `src/mcp/server.ts` — 5개 메모리 도구 등록
- `src/cli/index.ts` — memories 커맨드 + 온보딩 훅
---
